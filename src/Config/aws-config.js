import Credentials from "./credentials"

const config = {
    userPoolId: Credentials.USER_POOL_ID,
    region: Credentials.REGION,
    userPoolWebClientId: Credentials.APP_CLIENT_ID
    
};

export default config;